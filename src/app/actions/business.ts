'use server';

// File ini dikosongkan karena aplikasi telah beralih menggunakan Firebase Client SDK 
// untuk akses database Firestore secara langsung di sisi client.

export async function getBusinesses() {
  return [];
}
